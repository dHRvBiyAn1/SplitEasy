package com.spliteasy.entity;

/** What an expense was for. Drives the category icon in the UI; purely descriptive. */
public enum ExpenseCategory {
    FOOD_DRINK,
    GROCERIES,
    RENT_HOME,
    UTILITIES,
    TRAVEL,
    TRANSPORT,
    FUN,
    OTHER
}
